class Solution:
    def divide(self, dividend: int, divisor: int) -> int:
        k = int(dividend / divisor)
        if k < -2**31:
            k = -2**31
        if k > 2**31 - 1:
            k = 2**31 - 1
        return k
